# Time-Series-Library requirements aligned to the current `tslib_nightly` env.
# This remains a curated dependency list for the repo, not a full `pip freeze`.
# Versions below were refreshed from the `tslib_nightly` environment on 2026-03-27.
# Note: `torch.__version__` reports `2.11.0+cu130` in this env, while pip metadata resolves as `torch==2.11.0`.
# Note: `mamba-ssm` is installed from a local editable checkout in this env, but its version metadata is `2.3.1`.

# PyTorch stack
--extra-index-url https://download.pytorch.org/whl/nightly/cu128
torch==2.11.0
torchvision==0.26.0.dev20260315+cu128
torchaudio==2.11.0.dev20260315+cu128
triton==3.6.0+git9844da95
torchmetrics==1.9.0

# Core scientific stack
numpy==2.1.2
scipy==1.16.3
scikit-learn==1.7.2
pandas==2.3.3
matplotlib==3.10.8
sktime==0.40.1
sympy==1.14.0
PyWavelets==1.9.0
tqdm==4.66.5
patool==4.0.3

# Backbone and model dependencies
einops==0.8.1
local-attention==1.11.2
reformer-pytorch==1.4.4
datasets==4.5.0
mamba-ssm==2.3.1
tilelang==0.1.8
quack-kernels==0.3.1
ninja==1.13.0

# Hugging Face and zero-shot forecasting backbones
transformers==4.57.3
huggingface-hub==0.36.0
chronos-forecasting==2.2.1
tirex-ts==1.3.0
timesfm==1.3.0

# Uni2TS and related runtime dependencies
gluonts==0.16.2
lightning==2.6.0
pytorch-lightning==2.6.1
hydra-core==1.3.0
jax==0.8.1
jaxlib==0.8.1
jaxtyping==0.3.4
orjson==3.11.5
tensorboard==2.20.0
python-dotenv==1.0.0

# Optional Mamba acceleration
# `causal-conv1d` is not currently installed/importable in `tslib_nightly`.
# Install a compatible build separately if you want the fused causal-conv path:
# causal-conv1d>=1.4.0
